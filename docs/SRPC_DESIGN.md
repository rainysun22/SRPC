# SR-PC 落地设计文档（修订版 v1.9）

# 自省式预测编码（Self-Reflective Predictive Coding）演化智能系统

版本：v1.9 · 中文 · 2026
修订要点：v1.1 针对裂缝1-3 修订后，v1.2 响应五条硬伤（能量内生=形式约束 / GWT 局部化 / 阶段A即测免遗忘 / 近视期望自由能 / 证据三切分）；v1.3 修正 PCN 方向、升级自省为元认知、放宽帕累托、明示死亡二选一、补符号接地与新异性门控。
v1.4 决策变更（用户裁定）：**完全剔除现有大模型作为构建成分——大模型既不是"机械外围"，也不是"可借用工程手段"，更不是"基础设施回退"。大模型仅在能力评测中作为比较基准（yardstick）。** 由此：§5 决策门不再含"调用大模型"分支；§1.1-3 能力目标改为"以与大模型同基准对比达标的单一路线"；§2.2 机械外围只保留确定性 I/O；§6.1b 由"能效工具借用"改置为"能力比较基准"。
v1.5 实证状态回写（依据仓库实现与验收报告）：阶段 A 五条件全过（含信用分配早筛，3 seeds 全过）；阶段 B 主验收全过（能力上升/免遗忘/记忆增益/组合零样本；收尾待办=符号保真自测+迭代/稀疏预算记账，排期 W1）；阶段 C 软件版全过（C1 能力无回撤/C2 能效 3e6×/C3 密度 0.28/C4 int8 就绪），**硬件化部署锁住**（deferred 至有专用硬件）；承重墙判定**分支 A 初步成立**（单一原理可 Scale，规模兑现待 E2 缩放实验）；新增阶段 E/F/G 与排期（详见 docs/ROADMAP.md）；§8.5 信用分配早筛小节补全（原缺失）；§9 开放问题逐条更新状态。
v1.6 E0 启动记录（2026-09-07）：六项决策点拍板（E0 三配置矩阵 / E1 字节级 UTF-8 / E2 梯子+孪生 / F1 混合扩容 / G2 对照名单 / D 边界预登记）；tPC 锚点口径修正（BPC/BLEU）；GPU 依赖任务外部化（docs/GPU_TASKS.md）；执行约定 = 初衷三条不变（局部规则、免反传、结构稀疏），不可达即调研改方案。
v1.7 E0 收口回写（2026-09-07）：五配置矩阵 + 单头消融证据链（b1 零距离增益 −0.25% / 单头×b1 增益 −1.9% / a 输入层污染 −126%）判定 ARC-lite 记忆增益为**原理性不可达**（i.i.d. 任务信息性瓶颈，I(s_out; 原型|x_L)≈0），非架构债务；不变量 #2"能力=记忆·拼合"验证迁移 E3（语言流）/F3（ICL）；主线保持 base；债务 #1 关闭、#2 降优先级（§9-6，results_phaseB/report_e0.md）。
v1.8 E1 收口回写（2026-09-07）：字节级 UTF-8 词元前端（ByteTokenizer）+ 语言长程信用分配（assoc 延迟文本关联）**主验收 5/5 PASS**（acc 0.995 / hebb 0.177 / gap 0.818 / distal 0.440 / Δ=8 外推 0.985，3 seeds）；xorsum（4-bit 跨块 XOR）在线单样本学习不可达，按执行约定"不可达 → 调研论文改方案"处理——BP 在线对照同样失败 + batch 上界 1.0 → 归因 parity SQ-hard（在线样本效率瓶颈）而非局部规则原理，降为边界记录，修复排 F1 记忆回放（新增 §9-7；results_e1/report.md + e1_probe_{bp,protocol,boundary}.py）。
v1.9 E2 pilot 收口回写（2026-09-07）：µPC 参数化（锚点 h_ref=768 裁定 iters=12 / eta_w=0.01，BPC 4.529 下穿 unigram 4.797）+ 独立线性读出头（W_out+bias 承载 unigram 先验，修复 LS 读出归一化丢频率信息——频率信息不在参数而列幅度被归一化，§7.7 翻译器谱系）+ iPC 增量调度（增益 0.027 > 0.02 → 采纳）。**梯子 1/2/4M**：BPC 4.256 / 4.213 / 4.238（µPC 零调参迁移，transfer_gain −0.136 ≤ 0.03 PASS）；**BPTT 孪生**：3.500 / 3.464 / 3.530，PCN:孪生恒 1.20–1.22×。判据 2/3/4 PASS；判据 1（4M 单调性）pilot FAIL 与孪生同步回退 → 定性为数据量瓶颈（150k 步 vs 3.89M 参数 ≈ 每参数 0.15 次更新，终点差 0.025 < 振荡 ±0.3），**最终判定转 GPU 登顶跑全预算裁决（GPU_TASKS T1/T2）**（results_e2/report.md）。
v1.10 F1 收口回写（2026-09-09）：**F1a** n-gram 结构化慢记忆（把 E3 静态 unigram 升级为上下文相关条件字节先验）闭合 E3 判据①（旧语言回升 +38.2%→−8.5%，order≥1 达标）并放大判据②增益（+36.8%→+71.3%）；**F1b** VSA/HRR 绑定层（srpc/vsa.py，d≥512）确立组合口径——概念-关系组合查询容量-精度曲线单调（d=1024 k≤64 recall≥0.95）+ xorsum 组合记忆回放判据③（d=1024 全 seeds acc 0.73–0.78 ≥ 0.60），显式证伪类均值原型/成对累积批次两条直觉回放路径，论证"组合由记忆承担而非网络权重硬学"（能力=记忆·拼合）。详见新增 §9-7 与 results_phaseF/report_{f1a,f1b}.md。
  v1.11 F2 收口回写（2026-09-09）：课程级持续学习——把阶段 B 的 4 任务免遗忘推到 54 任务课程流（7 任务族、难度递增 0→4），对标 ContinuaFabric 同向验证。**判据 PASS（self 读出源）**：50+ 任务免遗忘，旧任务 forget_mean 0.037 ≤ 25%（3 seeds 全过）、逐格 acc 保留 ~100%。**可证伪机制结论**：课程级免遗忘上限 = 读出口所依赖的共享表征是否随课程漂移——recon/dual（含共享重建 W1@x1）下遗忘随课程长度单调累积（distance 0→53 回升 0.02→0.46）FAIL；把读出口放到 x_self（条件码 + 记忆先验直拉的自我层）则远期回升压平 ≤0.06 PASS——回答阶段 B"4 任务免遗忘能否规模化"之问：能，但需正确选择能力读出的承载层。PrototypeMemory 增量 0% 与 E0 原理自洽（含完备条件码的确定性任务上记忆先验冗余）。详见 §9-9 与 results_phaseF/report_f2.md。
  v1.12 F3 收口回写（2026-09-09）：上下文学习（ICL）等价验证——**闭合"有限经验→沉淀规律→组合生成未见结果"**（results_phaseF/report_f3.md）。颜色关系 ICL（8×8 网格、5 色置换 M=120、2 色网格）：few-shot 增益随示例数**单调严格上升**（acc: 0→0.047 / 1→0.146 / 2→0.515 / 3→0.844 / 4→0.946 / 6→0.994 / 8→1.000，5 seeds），记忆消融（k=0）0.047≈机会（≪few-shot）；**组合 r2∘r1**：两块独立 few-shot 恢复的关系拼接成新关系并作用到新探针，生成**训练中未出现的结果对**正确 0.961@pinned。**可证伪机制**：fast 记忆槽保存示例证据 → 一致性推断沉淀隐藏颜色置换（规律）→ 拼合应用至新探针。这也兑现 E0 迁移给 F3 的"映射随上下文变化 → 记忆有原理性增量"判语。F 阶段记忆价值验证全部收口（F1a/F1b/F2/F3）。详见 §9-10。

---

## 0. 文档目的与阅读对象

把"**从一条规则出发，通过与世界交互，长成同时具备智能 / 低功耗 / 能力（向大模型效果靠拢）／意识（结构上接近人脑）的系统**"这条路线，落地成可执行的、**无绑定返工**的架构与开发规范。

对象：架构师、算法工程师、研究负责人。用于统一路线、划定不变量、给出可证伪里程碑，避免在"当前达不到"的前提下误入死路。

---

## 1. 目标与北星

### 1.1 硬约束（缺一不可）

1. **智能**：预测、行动、学习闭环，能力随交互上升且**免遗忘**。
2. **低功耗**：能效是**架构内生的第一等约束**（从阶段 A 的**数学形式**即强制），而非后装优化。
3. **能力（v1.4 定版）**：目标仍是"接近大模型效果"（语言 / 知识 / 组合泛化），但**大模型只在能力评测中作为比较基准，绝不进入系统构造**。能否在大模型同一批 benchmark 上逼近/对等，由承重墙（§2.4 / §8.5）裁决能力上限；承重墙若不成立，则**下调能力预期为"脑式结构逼近"**（§5），而不是转而调用大模型。
4. **意识**：结构上具备意识理论指向的必要结构（自我模型、全局广播、整合度），**最接近人脑**。

> 可先达不到结果，但路线必须单调收敛、不逼回头。

### 1.2 北极星表述

> **在"单一原理"内自足长出的智能**：系统只有一条规则（最小化预测误差，含自省误差），**能力由同一个核心随交互长大，全程不依赖任何现有大模型**——大模型仅作为能力评测中的比较基准，不进系统、不借手段、不落为基础设施。能量、免遗忘、自省是**从阶段 A 起就是的硬约束**，四轴在分阶段路线中单调收敛。

### 1.3 路线正确性判据（三条，缺一不可）

1. **Pareto 单调**：每阶段只前进一个轴，其余不回撤。
2. **无绑定返工**：任何阶段都不落入"必须引入第二种原理 / 必须放弃一轴才能退出"的困局。
3. **产物可复用**：每阶段产物可被下阶段直接使用（无死锁 / 无返工）。

满足三条 => 单调收敛。**无绑定返工靠的是"尽早且廉价地检测最可能失败的点"**：阶段 A 即做信用分配早筛（§8.5）、免遗忘早验（§7.5-6）、动作可算性验证（§7.3-4），把最大风险在最低成本处排除。

> v1.5 状态：三项早验均已落地——信用分配早筛（§8.5）3 seeds 全过（分支 A 初步成立，§5）；免遗忘早验由阶段 B 顺序学习覆盖（旧任务回升均值 1.3%）；动作可算性由近视期望自由能 + 主动推理在 Track-1 实现。最大风险已按计划在最低成本处排除。

---

## 2. 核心架构：单一原理 + 机械外围

### 2.1 唯一规则

系统唯一内置能力是**最小化自由能（预测误差）**，分两路：
- **外部预测误差**：`e_out(t) = s(t) − ŝ(t)`
- **自省（元认知监控，v1.3）**：不再是"预测自己下一步隐状态"（那只是系统动力学建模，任何递归网络都有）。正确定义为监控**自身信念的精度 / 置信**——元精度 `ρ(t)`，以及二阶"信念-关于-信念"：系统表征"自己对自己当前内部估计的不确定性"。

自省 / 元精度回传修正自我模型、改变动作倾向。这一条同时驱动**感知 / 行动 / 学习 / 自知**。

### 2.2 单一核心 + 机械外围

- **单一核心（预测编码动力学）**：一切信念、认知、组合、自省能力全部在此原理内长大。
- **机械外围（I/O，无自主动力学）**：传感器 / 词元化器 / 序列化器 / 驱动 / §7.7 离散符号接地界面。只做**确定性、多对一、可逆**转换，不进主循环、不参与信念更新——**不存在"两个心智的接口"**。

> v1.4：机械外围**不含任何大模型**。词元化 / 序列化若需，一律用**本核心自训/自建**的确定性前端，而非现成预训练模型。

### 2.3 三个认知动词与同一机制

| 动词 | 机制 | 支柱 |
|---|---|---|
| **形成** | 感知-动作耦合中层级堆出结构表征 | 演化 |
| **修正** | 外部预测误差更新信念 | 智能（主线） |
| **组合** | 收敛信念进全局工作空间，工作记忆重组出复合概念 / 动作 | 能力 / 抽象 |

### 2.4 信用分配机制

关键区分：**朴素 Hebbian（相关性联想）≠ 误差驱动预测编码（PCN）**。
- 朴素 Hebbian 只在局部相关性上加强联，有语言 / ARC 天花板。
- 误差驱动 PCN：每个神经元仍只局部更新，但误差神经元在层间双向传播全局信息；稳定态是反传的近似——**长程信用分配可在同一原理内涌现**，无需外挂第二种原理。

这是"单一原理内长能力"的理论支点；能兑现到多大规模，是核心开放命题，由阶段 A 早筛先验证。

> v1.5 状态：早筛（§8.5）已验证 Δ=4 长程延迟下误差驱动显著优于纯相关（3 seeds 全过，acc 0.890 vs 0.512，gap 0.378），证明误差回传确实承载长程信用分配；**规模兑现**（语言 / ARC 级）由阶段 E2 / G2 缩放实验承接。

---

## 3. 宪法性不变量（不守即死路）

| # | 不变量 | 守住的目标 | 违背后果（死路形态） |
|---|---|---|---|
| 1 | **单一原理·尺度不变**：各规模、各层级同一套自由能预测更新 | 演化 | 引入第二种心智 / 微BP => 两半拼接（Frankenstein） |
| 2 | **能力=记忆·拼合**：知识活在学习进化的记忆里，靠组合而非囤参数 | 智能 + 能力 | 能力=参数量 => 效率化黑盒，演化主权丢失 |
| 3 | **能量内生·结构稀疏**：结构性稀疏（Top-K 激活 / 广播稀疏 / 分块稀疏权重 / **禁全连接 MatMul**）从阶段 A 起就是**数学形式**上的约束 | 低功耗 | 后装稀疏化（无论"过早"或"过晚"）=> 需重训 / 转换，能力回撤 |
| 4 | **自我模型·从一开始就在** | 意识 | 意识结构后贴 => 无法"接近人脑" |

> v1.2 释意（回应硬伤1）："能量内生"不是"记账指标"，而是**运算形态**。阶段 A/B 的核心算子（感知、生成、更新）从一开始就必须写成稀疏形式；阶段 C 只做**部署**，不做**改造**。见 §4.2 说明。

---

## 4. 分阶段路线与验收

### 4.1 阶段总览

```
北极星·单一原理单调收敛
   ↓
[A] 原理自证 + 信用分配早筛 + 免遗忘早验 + 结构稀疏内嵌（承重墙）     ✅ 完成（五条件全过）
   ↓
[B] 规模化与组合（智能 + 能力 · 保持结构稀疏 + 预算）               ✅ 主验收 3/4 · 收尾 W1 完成 · E0 收口（记忆增益=原理性不可达，验证迁移 E3/F3）
   ↓
[C] 硬件化部署（把已结构稀疏的核心落到事件驱动/低比特硬件，无架构变更）🔒 软件版 ✅ · 硬件部署锁住（deferred 至有专用硬件）
   ↓
[D] 意识向集成（自我模型 + GWT 广播 + 整合度 · 接近人脑）            📋 已记录，排期 W11-W12
   ↓
[E] 语言化（自建词元前端 + 语言级长程信用分配 + 缩放曲线）            📋 新规划，排期 W2-W6
   ↓
[F] 世界知识 + 持续学习（能力=记忆·拼合 实证 + 课程级免遗忘 + few-shot/ICL）        ✅ F1 已完成(W7) · F2 已完成(W8) · F3 已完成(W9)
   ↓
[G] 推理与规划（规划式未来预测器 + 推理闭环 + 同规模 LLM 对照）      📋 新规划，排期 W10-W11
```

> v1.5：E/F/G 详表（子阶段 / 分工 / 验收 / 排期）见 [docs/ROADMAP.md](ROADMAP.md)。C 硬件化在本路线图内锁住，不计排期。

### 4.2 能效两层含义（v1.2 新增，回应硬伤1）

"低功耗"要拆成两个不同对象，否则会自相矛盾：
- **结构性稀疏（形式层）**：Top-K 激活、广播/分块稀疏权重、禁用全连接 MatMul。**从阶段 A 起就是核心算子的数学形式**——这是不变量 #3 的所指，A/B/C 全程保持。
- **硬件实现（部署层）**：事件驱动 / 低比特 / 神经形态。**只在阶段 C 落定**——它把已稀疏的核心搬到省能物理载体，**不引入新的架构变更**。

> 因此阶段 A 用 CPU/GPU 跑原型 **不是"密集跑完后稀疏化"**，而是"**在软件里模拟稀疏形式的算子**"（数组底层可稠密，但定义运算是稀疏的，并计入迭代/稀疏记账）。硬伤1 说的"过晚改造"由此消除。

### 4.3 各阶段详表

| 阶段 | 证明什么 | 可证伪里程碑（不达标即回头信号） | 状态（v1.5） | 护栏回退 |
|---|---|---|---|---|---|
| **A 原理自证** | 结构性稀疏的单一规则在最小闭环长出"形成-修正-组合"；信用分配长程可回传；免遗忘可成立；自省环有增益；在迭代+稀疏预算内可收敛 | 早筛不过 / 健忘超标 / 迭代超预算 → 最小成本处止损 | ✅ 五条件全过（3 seeds） | 承重墙，先做透 |
| **B 规模化组合** | 能力随交互上升且免遗忘，在结构稀疏 + 预算内增长 | 组合/规划平顶返降，或预算超标 | **主验收 3/4**（能力上升 ✅ / 免遗忘 ✅ / 组合零样本 +26% ✅ / 记忆增益项经 **E0 收口重定性为原理性不可达**——i.i.d. 任务上记忆先验无增量信息，非架构债务，验证迁移 E3/F3，见 §9-6）；**收尾 W1 完成**：符号保真自测按在线阈值（v2）8/8 过（附 LS 上限对比），预算记账过（B1 口径修正 ≤3 迭代有效，>3 退化记债务 #2）；证据见 [report_w1.md](../results_phaseB/report_w1.md) / [report_e0.md](../results_phaseB/report_e0.md) | 回记忆拼合，重审能力来源 |
| **C 硬件化部署** | 把已结构稀疏核心部署到事件驱动/低比特硬件；**无架构变更** | 部署逼重训或能力回撤 | 🔒 软件版 ✅（C1-C4 全过）；硬件部署锁住，deferred 至有专用硬件 | 保留可回退 / 混合精度 |
| **D 意识向集成** | 自我模型、GWT 广播、整合度构成**可测结构**且随演化增强 | 结构指标≈随机 | 📋 已记录，排期 W11-W12 | 只报结构、不夸大圭性 |
| **E 语言化** | 单一原理在语言上逼近反传缩放曲线（对标 tPC 字节级 BPC 1.865 vs 1.864） | 缩放曲线显著劣于 BP 参考线 | 🚧 进行中（**E0 已收口**：记忆增益重定性为原理性不可达、验证迁移 E3/F3；**E1 已收口**：主验收 5/5 PASS + xorsum 在线边界归因（§9-7）；**E2 pilot 已完成**：µPC 迁移 + iPC 采纳 + 梯子/孪生判据 2/3/4 PASS，判据 1 转 GPU 登顶裁决，详见 ROADMAP） | 停语言化，回 B 收尾复查 |
| **F 知识+持续学习** | "能力=记忆·拼合"在课程级（50+ 任务流）不崩塌 *且* few-shot 可沉淀规律并组合生成未见结果 | 免遗忘上限提前暴露 / 组合召回不足 / ICL 增益不随示例上升 | 🔄 F1 ✅（n-gram + VSA/HRR 组合）、F2 ✅（54 任务免遗忘）、F3 ✅（ICL 关系恢复 + 组合）已收口，详见 ROADMAP | 回记忆拼合，重审能力来源 |
| **G 推理与规划** | 规划式未来预测器 + 推理闭环在同规模上与 LLM 对照 | 规划无增益 / 基准得分不对等 | 📋 排期 W10-W11（详见 ROADMAP） | 只报结构，不夸大能力 |

### 4.4 单调性检查（v1.3：允许有限抖动，只禁"不可逆回退"）

对指标向量 `（能力, 免遗忘程度, 能效[结构稀疏+迭代/稀疏预算], 自准确性）` 做检查：
- 允许 **≤5% 的短期抖动**，且须在 **≤10 步（阶段内）恢复**；
- 只有 **持续超过对称窗口、不可恢复的回退**才算"路线警告"，先解释修复再推进；
- 能效改造类阶段（如 C）天然伴随短期能力抖动，**免于被"零回退"误杀**。

> 释意（回应硬伤3）："无绑定返工"禁的是**不可逆、需推倒重做的回退**，不是"一时抖动"；严格零回退是不切实际的过度要求。

---

## 5. 死路与护栏

### 死路 1（v1.4 已彻底关闭）：把成熟大模型当"另一半心智"
- v1.1 曾改架构为"单一原理内长 + 机械外围"。**v1.4 用户裁定后此口彻底关闭**：大模型不进系统、不借手段、不落为基础设施，只在能力评测中作比较基准（§6.1b）。
- 残留护栏：即便外围与核心存在**数据格式偏置**风险，词元/序列化前端一律**自建**（§9.2），任何"写入状态 / 参与更新"的部分必须回到核心实现。

### 死路 2（限制收严）：为省能事后稀疏化（过早或过晚）
- 违反不变量 #3。**过晚（先稠密到 C 再变稀疏）与过早（没头绪就脉冲化）同样是死路**。
- 护栏：结构性稀疏是阶段 A 起的**形式约束**；C 只做硬件部署，不引入新性能牺牲；保留混合精度 / 可回退。

### 死路 3：信用分配早期不验，拖到语言规模才爆
- 护栏：阶段 A 即跑信用分配早筛（§8.5）；不达标立即回头。

### 死路 4（v1.2 新增）：GWT 作为"外来非局部插入件"
- 若强制加一个无法用局部更新规则实现的全局模块，违反不变量 #1。
- 护栏：GWT 必须给出**局部化更新公式**（§7.6），否则不算集成，只算拼贴。

### 能力定位的决策门（v1.4 定版，回应死亡二选一）

> v1.4 用户裁定：**大模型已从"可选加速器 / 必要基础设施"的所有定位中彻底剔除**，只剩"能力比较基准"一种身份。故前版"死亡二选一"的 **B1（调用大模型）分支已不可再选**——本路线**不存在**走回头路去调大模型的选项。

- **遗留真问题**：纯演化内核若无全局信用分配则无法 Scale 到语言 / ARC 级能力。此问题**无法再用"引入大模型"来兜底**。
- **化约为一个能力上限决策门**，由**承重墙**（§2.4 信用分配早筛 → §8.5）裁决：
  - **分支 A（承重墙成立，单一原理可 Scale）**：单一路线直达"接近大模型效果"，与比较基准对等——目标达成。
  - **分支 B（承重墙失败，单一原理规模天花板被证明确实存在）**：**下调能力预期**为"脑式结构逼近"（保留智能 / 低功耗 / 意识三轴，接受能力数值不对等大模型），并在 §9 明示该天花板作为公开结论——**绝不以任何形式回退为调用大模型**。
- 护栏：A/B 结果**提前声明为计划内分支**，能力目标在承重墙出结果前按"以比较基准衡量"处理（§1.1-3）；无论 A/B，**构造全程与大模型零耦合**，保证不会在阶段末爆出"不得不借大模型"的返工。

> **v1.5 裁决进度**：承重墙已出**初步结果——分支 A 成立**（§8.5：Δ=4 长程延迟下误差驱动 3 seeds 全过，acc 0.890 vs 纯相关 0.512，gap 0.378；远端块权重占比 0.409 > 机会水平）。"单一原理可 Scale"的承诺在原理层已兑现；**规模层兑现**（语言 / ARC 级匹敌比较基准）由阶段 E2 / G2 缩放实验承接，届时以同基准对比定论。分支 B 不触发，但不删除——若 E2 / G2 出现不可修复的规模天花板，按 §1.1-3 回退流程执行。

---

## 6. 证据分层（v1.2 三切分，回应硬伤5）

### 6.1a 本路线原则证据（唯一直接背书本路线的）
- **误差驱动 PCN 的局部塑性可近似反传**（Whittington & Bogacz 2017），小/中规模匹配 BP。这是承重墙（§2.4）的唯一实证支点。

### 6.1b 大模型 = 能力 / 能效**比较基准**（v1.4 重定义：只作度量对象，不作构建成分）
> v1.4：**现有大模型只作为本路线的测量基准（yardstick），不借用其任何技术、不进入系统。** 我们在**同一批 benchmark** 上与下列对象对比能力，并在阶段 C 与其报告的能效数字对比——用它们当"标尺"，而非"零部件"。它们全基于 Transformer + 全局反传，构成本路线的对照物，不构成本路线的支持证据。
- **能力对照**：在 ARC / 语言 / 组合泛化同基准上与主流 LLM（含 NSLLM、SpikingBrain、BitNet 水平模型）对比得分。
- **能效对照（阶段 C）**：以 NSLLM（FPGA 13.8W）、SpikingBrain2.0（神经形态 70% 面积 / 46% 功耗）、BitNet（能耗约 0.08× 参考线）的**已公布数字为实数参考**，检验本核心部署后的 TOPS/W、bytes/step 是否更优——**仅作对比线，不据此改造架构**。

### 6.1c 宏观战略佐证（说明"为何现在做"）
- **免遗忘 / 持续学习是当前公认前沿**：Ilya "scaling 结束、研究开始"；MIT / Tencent / Huawei 2026 独立收敛"持续学习是最优卡点"——这正是本路线核心所承担的方向。

### 6.2 靠原理推断、未来验证（核心命题，早筛先行）
- ③ 单一原理（误差驱动 PCN）能否从空白一路尺度不变地演化出语言 / ARC 级能力——尤其信用分配能否在规模上匹敌现代反传。**承重墙，阶段 A 早筛立项。**
- ④ 意识向结构能否收敛到"接近人脑"（结构验证可用，圭性不可承诺）。

> v1.6 状态：③ 的原理层部分已作答（§8.5 早筛 3 seeds 全过，分支 A 初步成立）；**规模层**（语言 / ARC 级）由阶段 E2 / G2 承接，以自训 BPTT 孪生为主参考线、tPC 字节级 BPC 1.865 为外部锚（v1.6 修正：tPC 原文口径为 BPC/BLEU，此前误记 PPL 7.62）。④ 排期 W11-W12（阶段 D）。

---

## 7. 阶段 A 最小原型规范（Phase-0）

### 7.1 运行环境（v1.2 澄清"模拟稀疏"）
- 纯软件：CPU / 单卡 GPU，Python + NumPy（可读、可复现）。
- 原型**模拟稀疏形式**：核心算子的数学形式 = 结构稀疏（Top-K / 广播稀疏权重）；数组底层可为稠密，但**定义运算必须稀疏**并计入记账（不变量 #3）。
- 不使用全局反传，不引入第二种学习原理；若引入需书面解释为何放松不变量 #1。

### 7.2 模块清单
1. **感官通路**：2 层级连续表征，低维环境感知（含可选机械外围：词元 / 序列化前端）。**表征自带 Top-K 稀疏。**
2. **层级生成模型（PCN 预测器）**：自上而下生成预测，自下而上只传误差；**误差神经元驱动更新**；权重分块/广播稀疏，**无全连接 MatMul**。
3. **自省环（元认知监控，v1.3）**：构建"元精度预测器"，追踪 `ρ(t)`（对自己预测可信度的置信），并产生二阶"信念-关于-信念"信号 `c(t)`，回传修正 + 影响动作。
4. **全局工作空间（GWT）**：见 §7.6 的**局部化实现**（MVP 先接一个"全局惊异标量"+ 高置信子集），为阶段 D 留接口。
5. **动作选择**：见 §7.3-4，用**近视期望自由能**（可算、局部、免未来展开）。

### 7.3 更新规则（局部：每次更新只依赖本地误差 + 至多一个广播标量）

```text
层级编号：L=0 输入（感受）向上增大；预测自上而下，残差自下而上（PCN 标准方向，v1.3 已更正）。

# 1) 推理（PCN 矢量动力学）：上层发"预测"，下层发"预测误差（残差）"
Pred_l      = f(W_l x_{l+1})               # 上方层对层 l 的预测（自上而下）
ε_l         = x_l − Pred_l                 # 层 l 的残差 = 预测误差
x_l ← x_l + a·ε_l − b·(W_{l-1})ᵀ·ε_{l-1} + γ·h
   #        ↑自残差   ↑"来自下方层的预测误差"回传   ↑GWT广播标量（§7.6）
   # 注意：从下方进来的是"残差 ε_{l-1}"，不是下方层的预测——见注解

# 2) 学习：局部、误差驱动、免反传，且带 Oja 归一化 + 侧抑制（防权重爆炸）
ΔW_l ∝ x_{l+1}·ε_lᵀ                          # PCN 局部规则
W_l ← 归一化( W_l + η·ΔW_l )                  # Oja / 权长归一
W_l ← 减去侧抑制方向( W_l )                   # 横向竞争，抑制自增长

# 3) 自省（元认知精度监控，非"预测自身隐状态"）
ρ(t)  = 精度( 自身预测可信度 )                 # 一阶：对自己猜得准不准的估计
c(t)  = ρ(t) − 预测(ρ(t+1))                   # 二阶"对自身置信变化的意外"——元认知监控信号

# 4) 动作（阶段A：近视期望自由能，含认知探索 + 新异性门控，防"剥削锁定"）
G_myopic(a) = Σ_n ‖e_n‖² / σ + β·(−Σ p log p) + κ·novelty(a)
μ = argmin_{a∈A} G_myopic(a)                  # 行动集 A 为有限/低维枚举
```

> **为何可算（回应硬伤4）**：阶段 A 不用全频域期望自由能（那需对未来多步概率展开 + 训练可微预测器，依赖反传/高阶规划）。此处 `G_myopic` 只依赖**当前层的预测误差与信念熵**——均为局部量；行动集低维，可直接枚举。全频域 / 规划式 G 与训练预测器统一推迟到阶段 B/D 视早筛结果再议。

> **方向注解（v1.3，回应错误1）**：层级预测编码中**方向严格单向**——上层只发"预测"、下层只发"残差"。节点更新里从下方进来的必须是**预测误差（残差 ε_{l-1}）**，绝不能把下方预测 raw 值当输入；上方则只接收残差汇总。此方向若不守，网络语义会退化为普通自编码，而非预测编码。

> **剥削锁定警告（v1.3，回应代码隐患 c）**：纯最小化"预测误差"会把系统推入**剥削态**——静止不动时环境最可预测、误差最小，探索会消失。因此智能增长不以裸误差下降为准，而用"已探索区域的信息增益 + 新区域访问率 + 新异性门控激活度"（§7.4）。`G_myopic` 已含 β·熵 与 κ·novelty 两项对冲。

> **硬性约束（回应硬伤1/裂缝3）**：单次推理迭代步数硬上限 `T ≤ 5`；只计算误差非零节点并计入记账。所有算子形式稀疏。

> **v1.5 适用范围裁定（早筛 vs 在线主路径）**：`T ≤ 5` 约束的适用对象是**在线交互主路径**（Track-1 感官通路的每步推理，`inner_iters=3` 满足）；**信用分配早筛（§8.5）属"承重墙诊断实验"，不在部署路径上**——为让误差信号沿长程延迟收敛，err 臂取 `settle_iters=32`、hebb 臂取 `hebb_settle_iters=8`。深迭代的代价**全额计入迭代/稀疏记账**（不逃避能效口径），且由阶段 E2 的缩放实验检验"收敛所需迭代数是否随规模可压缩"；若不可压缩，则作为能效权衡在 E2 显式汇报，不默认为满足。两类口径在记账表中分列，不混淆。

### 7.4 验证指标
- **形成**：层级是否自发结构化表征。
- **修正**：预测误差随交互下降；信念被新证据纠正。
- **探索（v1.3 新增，回应剥削锁定）**：开放 / 动态环境须监测**新异性门控激活度 + 新区域访问率**，并以"已探索区域的信息增益"计智能增长——不用裸误差下降（静止即可预测）。
- **组合**：跨模态 / 跨时段片段重组出新概念（合成数据）。
- **自省（元认知）**：元精度监控开 / 关对照，量化 `c(t)` 增益。
- **免遗忘（v1.2 新增）**：多任务顺次学习后，旧任务召回不显著下降。
- **能效**：结构稀疏 + 迭代预算 + 稀疏记账全部满足（先决条件）。

### 7.5 验收标准（阶段 A 通过条件）
1. **免反传**：全程无全局反传、在线增量运行。
2. **信用分配早筛**：长程依赖小任务中，误差能长程回传并驱动远端权重（§8.5）。
3. **形成 / 修正 / 组合**三指标达标。
4. **自省环**对照显示非零增益。
5. **能效**：在结构稀疏 + 迭代预算 + 稀疏记账内完成——"能量内生"在形式层成立，C 仅部署。
6. **免遗忘早验（v1.2 新增）**：多任务顺次召回达标 → 阶段 B 的主轴在 A 已提前验证，不满足即回退。全部满足才进入 B。

> **v1.5 验收结果（全部满足，3 seeds）**：
> 1. 免反传 ✅（Phase-0 全程局部误差更新，无 BP）
> 2. 信用分配早筛 ✅（§8.5：Δ=4 err 0.890 vs hebb 0.512，gap 0.378，distal 0.409，per-seed 全过）
> 3. 形成/修正/组合 ✅（形成：感受野余弦 0.559→0.979、捕获率 100%；修正：误差斜率 −3.3e−5；组合：跨片段重组零样本增益）
> 4. 自省环 ✅（重学误差较基线低 22%，非零增益）
> 5. 能效 ✅（形式层稀疏成立；在线主路径 T=3≤5；早筛深迭代全额记账，口径分列，见 §7.3）
> 6. 免遗忘早验 ✅（由阶段 B 顺序学习覆盖：旧任务回升均值 1.3%）
> 据此进入阶段 B（已完成）与后续 E/F/G。

### 7.6 GWT 的局部化实现（v1.2 新增，回应硬伤2）

关键原则：**规则局部 ≠ 连通局部**。一个单元可以拥有大量 fan-in 连接，只要它的**更新规则只依赖本地误差**，仍是局部更新。据此：

- **全局惊异标量（hub 单元 h）**：一个"全局枢纽单元"，fan-in 汇聚所有子模块的预测误差模二范：
  `h = Σ_n w_n · ‖e_n‖²`（w_n 为增益）。
  该 hub 自身的更新遵循 PCN 预测误差规则（它只是众多单元之一）。
- **广播调制**：把 `h` 作为**单个标量**广播给所有单元，进入各自的更新：`x_l ← … + γ·h`。
  每次更新仍只依赖本地误差 + 一个广播标量——**不是非局部权重矩阵**（回应"拉取-广播"无法局部实现的质疑）。
- **公共池竞争**：hub 的 fan-in 子集用 **k-winner-take-all** 选 top-k，由预测编码自主实现，无需外来模块。

> 验证：MVP 先用它接"高置信收敛信念子集"（§7.2-4），阶段 D 再扩展为真正的多模态工作空间。**给不出此局部化公式，则不宣称"集成 GWT"。**

### 7.7 离散符号接地机制（v1.3 新增，回应代码隐患 b）

**矛盾**：ARC 是**离散符号推理**（彩色方块网格 → 规则变换/泛化），而核心是**连续预测编码动力学**。若直接把连续信念当 ARC 的"符号"用，两者物理本质不兼容，评测会失真。

**解法：符号接地放在"机械外围"，不引入第二种核心原理**（守住不变量 #1）：
- **读方向（离散→连续）**：ARC 网格经**多对一、确定性、可逆**的编码器注入核心——每个离散符号（颜色/位置/排列）编码为高维**正交/分隔的基底向量**，喂给感官通路，作为待解释的观测 `s`。
- **写方向（连续→离散）**：核心收敛的连续信念，由同一个外围解码器在**离散基底上做 argmax/k-winner-take-all**，转回 ARC 的动作/网格。
- 关键点：**编码/解码都是无自主动力学的 I/O 映射**，接地发生在边界，不参与核心信念更新；离散组合结构（符号恒等、置换、重复）由基底的正交性显式承载，使核心能用连续动力学去平滑地逼近离散泛化，同时保持"单一原理长能力"不被破坏。

> 何时内置：此前端**从阶段 A 就预留接口**（§7.2-1 感官通路），在进入阶段 B 的 ARC 之前成套实现并自测"符号保真"（解码后符号不变），避免 B 阶段才发现连续/离散接口无法对接而返工。

> v1.5 状态：阶段 B 已以 **ARC 一热编码作为 §7.7 正交基底符号接地**接入（组合零样本 +26%）；**符号保真自测已执行**（B 收尾 W1，3 seeds）：阈值按在线可达重设（v2）8/8 过——S1 逐格一致率 min 0.927~0.953（LS 冻结解上限 0.987，在线 RLS 差距 2.6pp 为读出收敛残差）；S2 用基线偏差剥离一热背景公共模后位置/颜色区分 0.630/0.962、输入多样性 1.000；网格级一致率与块结构守恒为记录项（LS 上限 0.555 < 原阈值 0.90，任何读出算法不可达）。证据与债务清单见 [results_phaseB/report_w1.md](../results_phaseB/report_w1.md)。

---

## 8. 里程碑建议

| 阶段 | 主要工作 | 关键验收 | 状态（v1.5） |
|---|---|---|---|
| A | Phase-0 最小闭环 + 信用分配早筛 + 免遗忘早验 + 自省对照 + 稀疏预算 | §7.5 六项全过 | ✅ 全过（3 seeds） |
| B | 扩大规模 / 多时间尺度记忆 / 组合基准（ARC，先接 §7.7 离散符号接地）；规划式 G 视早筛而定 | 能力上升 + 免遗忘 + 预算不缺 + 符号保真 | **主验收 3/4**：能力/免遗忘/组合零样本过，记忆增益项经 E0 收口**重定性为原理性不可达**（i.i.d. 任务信息性瓶颈，非架构债务；验证迁移 E3/F3，§9-6）；✅ 收尾 W1 完成：符号保真自测（在线阈值 v2）8/8 过 + 预算记账过（B1 修正 ≤3 迭代有效，>3 退化记债务 #2，降优先级） |
| C | 已结构稀疏核心的事件驱动 / MatMul-free / 低比特**部署**（无架构变更） | 能力无回撤 + 能效 10–1000× | 🔒 软件版 ✅（C1-C4 全过）；硬件部署锁住 |
| D | 自我模型 + GWT 广播 + 整合度测量 | 结构指标随演化增强 | 📋 排期 W11-W12 |
| E | 语言化：自建字节级词元前端 + 语言级长程信用分配 + 缩放曲线（对标自训 BPTT 孪生；tPC 字节级 BPC 锚） | 语言长程达标 + 缩放曲线逼近 BP 参考线 | 🚧 E0 ✅ 已收口（原理性不可达 + 验证迁移 E3/F3，见 §9-6）；E1 ✅ 已收口（主验收 5/5 PASS + 在线组合边界归因，见 §9-2/§9-7）；E2 ✅ pilot 已完成（判据 2/3/4 PASS，判据 1 转 GPU 登顶裁决，见 §4.3/ROADMAP） |
| F | 知识 + 持续学习："能力=记忆·拼合"实证 + 课程级（50+ 任务流）免遗忘 + few-shot/ICL | 免遗忘上限达标 + 组合召回达标 + ICL 增益随示例数上升 | 🔄 **F1 ✅ 已完成**（2026-09-09）：F1a n-gram 结构化慢记忆闭合 E3 判据①（旧语言回升 ≤25%）+ 判据②增益放大；F1b VSA/HRR 绑定层确立组合口径（概念-关系组合查询 + xorsum 组合记忆回放判据③ d=1024 acc≥0.60）——见 §9-8；**F2 ✅ 已完成**（2026-09-09）：54 任务课程免遗忘 pass（self 读出源 forget_mean 0.037 ≤ 25%、acc 保留 ~100%；recon/dual 共享骨干漂移 FAIL、self 自我层压平 PASS）——见 §9-9；**F3 ✅ 已完成**（2026-09-09）：颜色关系 ICL few-shot 增益单调上升（acc(0) 0.047 → acc(8) 1.000，5 seeds；k=0 记忆消融≈机会），组合 r2∘r1 生成未见结果对正确 0.961@pinned，闭合"有限经验→沉淀规律→组合生成未见结果"——见 §9-10 |
| G | 推理与规划：规划式未来预测器 + 推理闭环 + 同规模 LLM 对照 | 规划有增益 + 同基准得分对齐 | 📋 排期 W10-W11 |

> E/F/G 子阶段、分工与验收明细见 [docs/ROADMAP.md](ROADMAP.md)。

### 8.5 信用分配早筛（承重墙）—— v1.5 补全正文（此前多处引用但缺本节）

**目的**：检测误差驱动 PCN 能否通过**局部规则**实现长程信用分配，区分于纯相关 Hebbian（§2.4 / §5 决策门唯一裁决依据）。

**任务（长程依赖小任务：延迟 XOR）**：
- 每步 `d_feat=2` 维特征，前两维为双峰 bit，其余为满幅随机干扰；目标 `y_t = XOR(bit0(x_{t-Δ}), bit1(x_{t-Δ}))`——只与 Δ=4 步前的输入有关，与任何单一输入特征**零边际相关**（纯相关联想必然失败）。
- 输入 = 最近 Δ+1 步窗口拼接（时间分块感受野：每个隐单元锚定一个时间块，1/(Δ+1) 单元看到远端块）；网络 `x0 → x1 → x2 → x3(one-hot 双输出)`，XOR 线性不可分 → 必须双输出单元。
- 评估协议"钳制-比较"（分别钳制两个候选，取自由能更低者），训练/评估同域。

**双臂对照（同网络同序列，仅学习规则不同）**：
- `error` 臂：规则 2 带误差加权（PCN 标准局部规则，信用分配）；
- `hebb` 臂：规则 2 为纯相关性（无误差加权，朴素联想，XOR 有天花板）。

**验收判据（全部 per-seed 满足，非仅均值）**：`acc_pcn ≥ 0.80`（机会 50%）、`acc_hebb ≤ 0.68`、`acc_gap ≥ 0.30`、`distal_share ≥ 0.25`（远端块权重变化占比，机会水平 20%）。

**实现结果（v2 收敛配置，3 seeds 全过）**：
| seed | err(Δ4) | hebb(Δ4) | gap | distal | err(Δ1) |
|---|---|---|---|---|---|
| 0 | 0.880 | 0.528 | 0.352 | 0.480 | ≥0.75 |
| 1 | 0.837 | 0.526 | 0.311 | 0.404 | ≥0.75 |
| 2 | 0.954 | 0.483 | 0.471 | 0.342 | ≥0.75 |
| 均值 | 0.890 | 0.512 | 0.378 | 0.409 | ≥0.75 |

**方差修复关键（种子间性能方差收敛配置，回写于 `srpc/config.py::CreditConfig`）**：
- 0/1 二值 bit 编码（原 0.2/0.8）：低输入强度位型 (0,0) 的远端感知弱是 seed1 掉队根因（位型准确率 0.425 < 随机）；双峰距离最大，XOR 语义不变；
- α=1.5 > β=1.0：增强顶层类拉动（类质心坍缩缓解，W3 类原型列近正交）；
- err 臂深迭代收敛（`settle_iters=32`, `eta_inf=0.09`）；hebb 臂浅迭代（`hebb_settle_iters=8`）+ 自由推断训练（`hebb_free=True`，仅输入驱动表征，基线贴近随机 ~0.51，gap 判据成立）；
- 侧抑制（orth）由出生时 W3 类原型列一次性正交化构造，评估期重复 Gram-Schmidt 会使 err 臂退化（0.75-0.77），故默认关闭；
- 迭代/能耗口径：早筛深迭代全额计入记账，与在线主路径 T≤5 分列（§7.3）。

**结论**：承重墙通过 → §5 决策门分支 A 初步成立；规模层兑现由 E2 / G2 承接。

---

## 9. 开放问题与会后决策

1. **信用分配兑现度**：PCN 在语言 / ARC 的信用分配能否匹敌现代反传 — 由阶段 A 早筛定生死，需量化阈值。
2. **自建词元前端（v1.4；✅ E1 已落地 2026-09-07）**：序列化 / 词元化前端由核心自训自建（确定性 I/O），**不含任何现成预训练模型**。已实现 = ByteTokenizer（srpc/lang.py）：text ⇄ UTF-8 字节 ⇄ 256 维一热（正交基底，与 ARC 符号编码 §7.7 同构），7 组多语/emoji/控制字符往返质检全过；E2 语言流直接复用。语言长程信用分配（assoc 延迟文本关联，16 类零边际相关）主验收 5/5 PASS：acc 0.995 / hebb 0.177 / gap 0.818 / distal 0.440（Δ=4）/ Δ=8 外推 0.985（3 seeds，results_e1/report.md）；**在线组合信用分配（xorsum）不可达已归因并排期修复，见 §9-7**。
3. **规划式 G / 未来预测器**：是否在阶段 B 引入（需视早筛），与"免反传"如何调和。
4. **意识层级**：阶段 D 为结构性验证，圭性不做承诺。
5. **优先级**：先推进阶段 A 原型，还是先细化 §7.5 六项早筛的量化设计。
6. **记忆-读出耦合架构（v1.5 B 收尾新增，✅ E0 已收口 2026-09-07）**：原假设 = 记忆先验只拉顶层 x_L、读出头源在底层 ŝ=W1@x1、中间迭代动力学不下传（架构性债务）。**E0 矩阵 + 单头消融证据链推翻架构假设**：b1（读出源切 x_L，零传播距离）增益 −0.25%；单头（head_idx≡0，遗忘压力最大化）× b1 增益 −1.9%；a/ab（x1 级记忆）−126% 且 S1/S2 回退（输入层先验污染证据）。**重定性**：ARC-lite retain 为 i.i.d. 确定映射，x_L 已编码 (s_in, cond)，I(s_out; 记忆原型 | x_L) ≈ 0——变换知识活在 W_outs 参数里，记忆增益 ≥10% 是**任务性质决定的原理性不可达**，任何耦合/下传设计均不可达。**初衷不变下的方案更改**：不变量 #2"能力=记忆·拼合"的实证验证迁移至记忆有原理性增量信息的任务——E3 语言流（当前 token 不含全部上下文）与 F3 ICL（映射随上下文变化）；ARC-lite 上记忆机制保留（免遗忘结构仍工作）但不再以 retain 增益为验收口径。主线保持 base（recon 源 + RLS）；b2 双源组合增益 57%（vs base 26%）记观察项供 E1 读出设计重估。证据 = [results_phaseB/report_e0.md](../results_phaseB/report_e0.md)（e0_matrix.json / e0_ablate_singlehead.json）。
7. **E1 在线组合信用分配 & F1 结构化记忆修复（v1.9，✅ 2026-09-09）**：① 归因——xorsum（4-bit 跨块 XOR，16 类）在线单样本不可达：BP 在线对照同样失败（parity 为 SQ-hard 高频函数，Kearns & Valiant 1989） + batch 上界 1.0（mini-batch SGD 可达）→ 瓶颈为在线协议样本效率 + 结构稀疏组件（k-WTA/clip/列归一）破坏 PCN 收敛态≈BP 的 batch 等效，**排 F1 记忆回放修复**（e1_probe_{bp,protocol,boundary}.py）。② **F1 收口（results_phaseF/report_{f1a,f1b}.md）**：F1a 把静态 unigram 慢记忆升级为上下文相关 n-gram 条件字节先验，闭合 E3 判据①（旧语言回升 +38.2%→−8.5%）并放大判据②增益（+36.8%→+71.3%）——即"防遗忘缺口 = 记忆载体信息不足（缺上下文维度）而非记忆量不够"。③ **F1b 用 VSA/HRR 绑定层确立组合口径**：概念-关系组合查询（循环卷积 bind/unbind + cleanup，d≥512）容量-精度曲线单调（d=1024 k≤64 recall≥0.95，Plate ~O(d/4-8)）；**xorsum 组合记忆回放判据③ d=1024 全 seeds acc 0.73–0.78 ≥ 0.60**——并显式证伪两条直觉回放路径（类均值原型回放：给定 y 时远端块 marginal≡全局 uniform、一阶均值零判别信息，原理性失败；真·成对累积批次回放：PCN 稀疏组件无法在权重吸收 parity）。**结论**：组合查询的答案由**记忆承担**（绑定叠加固化 → 拼合算子回答）而非网络权重硬学，正是不变量 #2"能力=记忆·拼合"。实现 = srpc/vsa.py（VsaMemory + bind/unbind）+ e3/f1b_*.py；结果 = e3/results_f1b_xorsum_memory.json。
8. **记忆增益普适性（v1.10，✅ F1 已收口 2026-09-09）**：把"记忆只在无完备上下文的任务上有增量"这一 E0 结论推向结构化记忆——F1a 与 F1b 均在**语言流/组合查询**（输入不自动携带全部输出信息）上验证记忆价值并放大增益，倒逼"防遗忘缺口"从记忆量转到记忆载体信息维度（n-gram 上下文）、从网络硬学转到记忆拼合（VSA 绑定）。**边界的另一面在 F2 得到 54 任务规模重现**（见 §9-9）：给定完备条件码的确定性映射上，记忆原型增量仍为 0%——一致成立。
9. **课程级免遗忘上限（v1.11，✅ F2 已收口 2026-09-09）**：把阶段 B 的 4 任务免遗忘推到 **54 任务课程流**（results_phaseF/report_f2.md）。**判据 PASS（self 读出源）**：forget_mean 0.037 ≤ 25%（3 seeds）、逐格 acc 保留 ~100%、记忆结构扩容到 54 组正常。**可证伪机制结论**：课程级免遗忘上限 = **读出口所依赖的共享表征是否随课程漂移**——recon/dual（读出口含共享重建 W1@x1）下远期任务（distance 36–53）回升 0.46、随课程长度单调累积（distance 0→53 回升 0.02→0.46）FAIL；把读出口放到 x_self（条件码 + 记忆先验直拉的自我层）远期回升压平 ≤0.06 PASS。即"50+ 任务免遗忘的关键不在每任务片段使其免遗忘，而在于**能力读出所锚定的那层表征不被后续课程漂移**"。这也回答阶段 B 遗留的"4 任务免遗忘能否规模化"：能，但需正确承载层。PrototypeMemory 增量 0% 与 E0 原理自洽（完备条件码确定性任务记忆冗余）——记忆价值验证路径保持 E3/F3。
10. **上下文学习（few-shot）与组合生成（v1.12，✅ F3 已收口 2026-09-09）**：兑现 E0 迁移给 F3 的判语——在"映射随上下文示例变化"的任务上验证 fast 记忆的原理性增量，并闭合「有限经验→沉淀规律→组合生成未见结果」（results_phaseF/report_f3.md）。**任务**：颜色关系 ICL（8×8 网格、5 个活动色、隐藏关系 = 120 种颜色置换之一，探针输出对不在示例中）。**机制**：示例对进 fast 记忆槽 → 一致性推断沉淀隐藏置换（规律）→ 拼合应用到新探针。**结果**：few-shot 增益随示例数**单调严格上升**（acc: k0=0.047 → k1=0.146 → k2=0.515 → k3=0.844 → k4=0.946 → k6=0.994 → k8=1.000，5 seeds 全过）；记忆消融（k=0）0.047≈机会（1/120）≪few-shot，是"规律沉淀的必要载体"的直接证据；**组合 r2∘r1**（两块独立 few-shot 恢复的关系拼接成新关系）在探针上生成**训练中未出现的结果对**正确 0.961@pinned。**可证伪结论**：记忆价值存在恰在"输入不含全部输出信息、需从有限上下文示例重建关系"的任务类；这与 E0/F2 的"完备条件码确定性任务记忆冗余"互为边界，构成"能力=记忆·拼合"在 few-shot 规模的完整实证。F 阶段记忆价值验证全部收口（F1a/F1b/F2/F3）。

> **v1.5 状态逐条**：
> 1. **部分作答**：原理层已答（§8.5 早筛 Δ=4 全过，分支 A 初步成立）；**语言 / ARC 规模层**由 E2 / G2 承接（主参考 = 自训 BPTT 孪生，外部锚 tPC 字节级 BPC 1.865 / BLEU 20.23；对照名单已拍板：孪生 + pythia-14m/31m + tPC 家族）。
> 2. **已拍板（2026-09-07）**：阶段 E1 词元化 = **字节级 UTF-8**（tPC 同口径、256 维一热即正交基底与 ARC 编码同构、E3 多语原生覆盖；子词弃）。
> 3. **已立项**：阶段 G1（排期 W10-W11）；早筛已过 → 立项条件满足；以"自省环前向展开"实现，与免反传调和（不引入 BP 式规划梯度）。
> 4. **已排期**：阶段 D（W11-W12），结构性验证口径不变。
> 5. **已定**：A 原型与早筛量化设计均已完成（§7.5 / §8.5）；当前优先级 = B 收尾（W1 ✅ + E0 收口 ✅）→ E1/E2/E3/F/G（W2-W11）→ D（W11-W12），见 [docs/ROADMAP.md](ROADMAP.md)。
> 6. **✅ 已收口（2026-09-07）**：E0 记忆-读出层稳定下传路径设计——五配置矩阵 + 单头消融判定为**原理性不可达**（信息性瓶颈，非架构性），验证迁移 E3/F3（详见 §9-6 与 [results_phaseB/report_e0.md](../results_phaseB/report_e0.md)）；B 收尾冻结证据（读出头权重逐元素相同、层间混沌追踪、谱测量 birth 47.97→trained 3.00、LS 上限 cell 0.987/grid 0.555）见 [results_phaseB/report_w1.md](../results_phaseB/report_w1.md)。

---

## 附录 A：术语表
- **SR-PC / 自省式预测编码**：单一原理核心 = 标准预测编码（PCN）+ 自省环（自我模型）。
- **误差驱动 PCN**：单元局部更新，但误差神经元层间双向传播全局信息，稳定态近似反传（区别于朴素 Hebbian）。
- **结构性稀疏**：Top-K 激活、广播/分块稀疏权重、无全连接 MatMul——阶段 A 起就是核心算子的数学形式（不变量 #3）。
- **机械外围**：无自主动力学的确定性 I/O 转换（传感器 / 词元化 / 序列化），非心智。
- **GWT / 全局工作空间**：收敛信念被稀疏广播、驱动决策的机制；本方案以"hub 全局惊异标量 + 广播调制 + k-winner-take-all"做**局部化实现**（§7.6）。
- **近视期望自由能 G_myopic**：只依赖当前层误差与信念熵的、可局部计算的行动代价，阶段 A 用它替代不可算的全频域 G。
- **自由能 / 主动推理**：以最小化预测误差为唯一原则的统一感知-行动-学习框架（Friston）。
- **免遗忘**：持续学习中吸收新知识而不破坏旧知识。**信用分配早筛**：阶段 A 检测长程依赖能否由误差机制回传的福德性小实验。

## 附录 B：参考（选要）
- Whittington & Bogacz（2017）：An Approximation of the Error Backpropagation Algorithm in a Predictive Coding Network with Local Hebbian Synaptic Plasticity
- SpikingBrain: arXiv:2509.05276；SpikingBrain2.0: arXiv:2604.22575；NSLLM: National Science Review 13:nwaf551；Loihi2 LLM: arXiv:2503.18002
- Self-Distillation Enables Continual Learning (MIT): arXiv:2601.19897
- Continual Learning as the next scaling frontier: MIT / Tencent / Huawei 2026；Ilya Sutskever 访谈
- **未来对标参考线（v1.5，只作测量标尺，不借技术进入系统）**：
  - tPC + RTRL（Potter & Rhodes 2026）：字节级 WikiText-103 BPC 1.865 vs BPTT 1.864（持平）；CCMatrix 英法翻译（15M 级）BLEU 20.23 vs 20.29 —— 阶段 E2 外部方向锚（主参考 = 自训 BPTT 孪生；v1.6 修正口径误记）；
  - µPC（Innocenti et al. 2025）：可扩展参数化，100+ 层稳定训练 —— 阶段 E2 深度稳定方案对照；
  - iPC（Salvatori et al., ICLR 2024）：增量调度训条件 / 掩码语言模型，更稳更快 —— 阶段 E2 调度方案对照；
  - ContinuaFabric（2026）：JAX PC 引擎，局部 Hebbian 学习天然持续学习 —— 阶段 F2 同向验证参照。
